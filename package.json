{
  "name": "root",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "test": "lerna run test --stream -- --colors",
    "lint": "lerna run lint",
    "reset": "bin/clean-all-modules.sh && lerna bootstrap"
  },
  "license": "MIT",
  "dependencies": {
    "yarn": "^1.22.4"
  }
}
