{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.4.3"
  },
  "workspaces": {
  "packages": [
    "packages/*"
  ]
},
  "scripts": {
    "test": "lerna run test --stream -- --colors",
    "re-install": "scripts/clean-all-modules.sh && lerna bootstrap"
  }
}
