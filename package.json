{
  "name": "root",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "test": "lerna run test --stream -- --colors",
    "test-only": "lerna run test-only --stream -- --colors",
    "lint": "lerna run lint",
    "reset": "bin/clean-all-modules.sh && lerna bootstrap",
    "push": "bin/pre-push.sh && gamp.sh $1"
  },
  "license": "MIT",
  "dependencies": {
    "yarn": "^1.22.4"
  }
}
