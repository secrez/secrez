{
  "name": "root",
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "test": "lerna run test --stream -- --colors",
    "re-install": "bin/clean-all-modules.sh && lerna bootstrap",
    "secrez:dev": "cd packages/secrez && npm run dev"
  },
  "license": "MIT",
  "dependencies": {
    "yarn": "^1.22.4"
  }
}
